# Database (Neon PostgreSQL)
DATABASE_URL="postgresql://user:password@host:5432/gympro?sslmode=require"

# This file lists ALL env vars needed across the monorepo.
# Copy to .env and fill in your values.
# Each app also has its own .env.example with app-specific vars.
